# firebase-functions@4.x declares peer firebase-admin only up to v12; this project
# uses firebase-admin@13 (supported by firebase-functions-test and runtime). npm
# would otherwise refuse to resolve the tree — see npm ERESOLVE.
legacy-peer-deps=true
